Google Highlights
<% stats = @account_group.overall_stats(@session["date"], "Google") %>
Total Visits: <%=stats[:v][0]%> - <%=stats[:v][2]%> last month (<%=stats[:v][1]%>)
Total Sales: <%=stats[:s][0]%> - <%=stats[:s][2]%> last month (<%=stats[:s][1]%>)
Total Cost: <%=number_to_currency stats[:c][0]%> - <%=stats[:c][2]%>  last month (<%=number_to_currency stats[:c][1]%>)
Cost per Click: <%=number_to_currency stats[:cpc][0]%> - <%=stats[:cpc][2]%> last month (<%=number_to_currency stats[:cpc][1]%>)
Cost per Sale: <%=number_to_currency stats[:cps][0]%> - <%=stats[:cps][2]%> last month (<%=number_to_currency stats[:cps][1]%>)
